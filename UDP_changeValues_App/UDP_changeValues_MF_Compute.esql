
DECLARE DSN EXTERNAL CHARACTER 'DSN_2';
CREATE COMPUTE MODULE UDP_changeValues_MF_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		-- CALL CopyMessageHeaders();
		 CALL CopyEntireMessage();
		RETURN TRUE;
	END;
               DECLARE cha SHARED CHARACTER;

	CREATE PROCEDURE CopyEntireMessage() BEGIN
		
	DECLARE QUERY CHARACTER 'SELECT * FROM EMP';
	SET OutputRoot.JSON.Data.RESULT = PASSTHRU (QUERY.DSN_2);
	
	END;
END MODULE;
